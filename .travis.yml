
language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"

# command to install dependencies
install: pip install -r requirements.txt

# command to run tests, e.g. python setup.py test
script: python tests/test_basic.py

branches:
  only:
    - master